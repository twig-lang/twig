fn factorial_iter | mut x: u64 | : u64 = (
  let result = mut 1;

  while x > 2 do (
    set result = result * x;
    set x = x - 1
  );

  result
);

fn factorial_rec | x: u64 | : u64 =
 if x < 2
  then x
  else x * factorial_rec(x - 1);
