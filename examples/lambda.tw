fn pass | value: i32,
          to: fn |i32| i32 | =
 to: value;

fn passed: i32 = (
 let lambda = fn |x| (2*x);
 pass(6): lambda
);

fn has_capture: i32 = (
 let my_value = 100;

 { my_value is "captured" by lambda }
 let lambda = fn | _ | my_value;

 pass(6): lambda
);

fn fn_pointer | f: fn* |i32| | =
 f: 0;

fn passed_pointer = (
 { function pointers cannot capture variables }
 let lambda = fn* | _ | ();
 fn_pointer: lambda
);
