type Bit = enum {
  Set;
  Clear;
};

fn not | x: Bit | : Bit =
 match x {
  Set = Clear;
  Clear = Set;
 };

fn and | l: Bit, r: Bit | : Bit =
 if l = Set && r = Set then Set else Clear;

fn or | l: Bit, r: Bit | : Bit =
 if l = Set || r = Set then Set else Clear;
